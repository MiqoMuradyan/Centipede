#ifndef ENUMS_HPP
#define ENUMS_HPP

enum ECharactersType {
    blank,
    player,
    centipede,
    mushroom,
    spider,
    bullet
};

enum EDirections {
    up,
    down,
    right,
    left
};

#endif //ENUMS_HPP